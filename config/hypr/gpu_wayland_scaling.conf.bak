# =============================================
#   archenemy - gpu_wayland_scaling.conf (warstwa WSPÓLNA)
#   Uniwersalne zmienne środowiskowe Wayland/skalowanie.
#
#   Zmienne zależne od karty graficznej (np. NVIDIA)
#   są per-maszyna i lądują w generowanym pliku
#   gpu-env.conf (tworzy go install.sh po wykryciu GPU).
# =============================================

env = QT_QPA_PLATFORM,wayland;xcb
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = MOZ_ENABLE_WAYLAND,1
env = ELECTRON_OZONE_PLATFORM_HINT,auto
env = XDG_SESSION_TYPE,wayland
env = XDG_CURRENT_DESKTOP,Hyprland
env = XDG_SESSION_DESKTOP,Hyprland
env = GDK_SCALE,1
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = XCURSOR_THEME,Adwaita
env = STEAM_FORCE_DESKTOPUI_SCALING,1
