# Gry: tearing (immediate) = minimalny input lag (master toggle: general:allow_tearing)
# steam_app_<id> = gry Steam/Proton — wcześniej nie łapała ich żadna reguła
windowrule = match:class ^(gamescope)$, immediate on
windowrule = match:class ^(.*\.exe)$, immediate on
windowrule = match:class ^(steam_app_\d+)$, immediate on

# Gry: content type "game" — włącza auto-tryb cursor:no_break_fs_vrr
# (ruch myszy nie wymusza dodatkowych klatek w fullscreenowej grze z VRR = brak skoków frametime)
windowrule = match:class ^(steam_app_\d+|gamescope|.*\.exe)$, content game

# Gry: zero efektów rice'a na oknie gry — parytet z betą (tam gry działały dobrze):
# bez blura, cieni, przyciemniania i animacji; okno w pełni nieprzezroczyste
windowrule = match:class ^(steam_app_\d+|gamescope|.*\.exe)$, no_blur on
windowrule = match:class ^(steam_app_\d+|gamescope|.*\.exe)$, no_shadow on
windowrule = match:class ^(steam_app_\d+|gamescope|.*\.exe)$, no_dim on
windowrule = match:class ^(steam_app_\d+|gamescope|.*\.exe)$, no_anim on
windowrule = match:class ^(steam_app_\d+|gamescope|.*\.exe)$, opaque on
windowrule = float on, match:class ^(MEGAsync)$
windowrule = float on, match:title ^(Set up MEGA)$


